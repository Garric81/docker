FROM python:alpine #  обаз и тег
WORKDIR /app  # создание папки  внутри  образа
COPY . . #  копирование  с текущей  авки  в докер
CMD [ "python", "main.py" ]  #  копирование фала и  запуск  фала  из папки app пункт выше
###################################################################
Создание  нового образа
Docker build . ( текущая  директория  где лежит  докер файл)
docker -f 

Docker build . -t  my_nginx:1.1.1 # Название  образа и тег
Docker run my_nginx 

##################################################################
 docker  run -it my-calendar

